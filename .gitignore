# IntelliJ / IDEA
.idea/
*.iml
*.iws
out/

# Eclipse
.classpath
.project
.settings/

# Gradle
.gradle/
build/
build_manual/
!gradle/wrapper/gradle-wrapper.jar

# Maven
target/
pom.xml.tag
pom.xml.releaseBackup
pom.xml.versionsBackup
pom.xml.next
release.properties

# OS specific
.DS_Store
Thumbs.db

# Dependencies (Raw JARs)
libs/

# Temp
*.tmp
*.bak
*.swp
*~.nib
*:Zone.Identifier
*Zone.Identifier

# Compiled class files (never tracked; build output goes to build_manual/)
*.class

# Secrets / keys — never commit
.env
.env.*
*.pem
*.key
*.jks
*.keystore
*.p12
*.pfx
*.pkcs12
id_rsa
id_ed25519
*.token
credentials*.json
secrets.*

# Graphify knowledge-graph caches & dated snapshots (ephemeral, regenerated on scan)
graphify-out/cache/
graphify-out/202*/

# Agent working notes / scratch
HANDOFF*.md
/Main*.java

# Anthropic Claude
.claude/
PLAN.md
# NOTE: CLAUDE.md is intentionally tracked — it is project instructions checked
# into the repo, not local scratch. Do not add it back here.
# Added by code-review-graph
.code-review-graph/

# Debug build (local only)
icarus-extension/build_debug.sh
