package icarus.report.model;

/**
 * Finding fields that can be included or omitted via ContentPolicy.
 */
public enum FindingField {
    WHY,
    WHEN,
    WHERE,
    HOW,
    IMPACT,
    DESCRIPTION,
    REMEDIATION,
    EVIDENCE
}
